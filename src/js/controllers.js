import kirby from "./kirby";


const controllers = {
    init(game){
        window.addEventListener('keydown', (e)=>{
            if (e.key === ''){


            }
        })
    }
}
export default controllers